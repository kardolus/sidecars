# Cloud Foundry Sidecar Sample App

## Installation
Build the sidecar by executing `./scripts/install_sidecar.sh`. 

## Deploy to CF
Look at the commands outlined in `./scripts/deploy.sh` or just run the script :) 
